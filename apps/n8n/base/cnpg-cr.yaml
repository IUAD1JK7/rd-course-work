apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: example-postgres
spec:
  instances: 1
  postgresql:
    version: "16"

  storage:
    size: 1Gi

  bootstrap:
    initdb:
      database: placeholder
      owner: placeholder
      secret:
         name: n8n-db-user-secret
