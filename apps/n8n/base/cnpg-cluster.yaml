apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres-db
spec:
  instances: 1

  storage:
    size: 1Gi

  bootstrap:
    initdb:
      database: placeholder
      owner: placeholder
      secret:
         name: n8n-db-user-secret
